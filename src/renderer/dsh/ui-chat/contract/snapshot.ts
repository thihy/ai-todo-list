/** Local re-export shim for `AssistantBlock` — see ./slots.ts for why the real
 *  ui-chat contract subtree is not vendored. */
export type { AssistantBlock } from '@deepseek-ai/dsh-client-ui-chat/client'
